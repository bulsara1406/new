pipeline {
    agent any
    stages {
        stage('Clone Repository') {
            steps {
                git branch: 'main', 
                    url: 'https://github.com/jenkins-docs/simple-java-maven-app'
                echo 'Repository cloned successfully'
                sh 'ls -la'  // Lists cloned files
            }
        }
    }
}
